# 解释器: D:\桌面\地质\大创\dataAnalysis\.venv\Scripts\python.exe  (Python 3.14.5)
# 本文件记录当前环境已验证的版本；重建环境: pip install -r requirements.txt
# 注意: Python 3.14 下无 pyarrow/pyyaml 发行版，项目约定 CSV + JSON 替代。
torch==2.13.0+cpu      # CPU 版即满足本项目规模；如需 GPU 版另行安装
numpy==2.5.1
pandas==3.0.3
scikit-learn==1.9.0
matplotlib==3.11.0
openpyxl==3.1.5
