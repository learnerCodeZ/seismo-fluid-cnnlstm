# 阶段 0 笔记：环境搭建（已完成，待检查）

> 完成日期：2026-09-03 ｜ 执行人：ZCode + 舒焕团队 ｜ 对应《项目实施指南》阶段 0

## 完成内容

1. **代码仓建立**：`D:\MYCODE\dachuang_cnn_lstm\v6.0_src\`（全英文路径、无空格），目录骨架与《实施指南》§6 一致：`src/ configs/ data/clean/ results/ figures/`。
2. **解释器决策**：复用文档仓既有环境 `D:\桌面\地质\大创\dataAnalysis\.venv\Scripts\python.exe`（Python 3.14.5）。检查发现 **torch 2.13.0+cpu、scikit-learn、pandas、matplotlib、numpy、openpyxl 均已装好**，无需重装，节省磁盘（D 盘剩余 96G）。
3. **两个依赖的替代决策**：Python 3.14 太新，`pyarrow`/`pyyaml` 无对应发行版。决定：**清洗数据用 CSV（放弃 parquet）、配置用 JSON（放弃 yaml）**——数据总量仅几 MB，无性能损失。此决策已回写进《实施指南》。
4. **核心工具模块 `src/common.py`**：
   - `set_seed(seed)`：固定 random/numpy/torch 种子；
   - `load_paths()`：读取 `configs/paths.json`，external（原始数据，相对文档仓 data_root，只读）/ internal（产物，相对代码仓根，自动建目录）两组；
   - `load_config(name)`：实验配置加载入口（阶段 2 起使用）。
5. **`requirements.txt`**：记录当前已验证版本及解释器位置。

## 验证结果（均通过）

- 8 个路径全部解析正确且存在（原始数据 5 个只读 + 产物目录 3 个自动创建）；
- import torch/sklearn/pandas/numpy 全通过；
- 种子复现测试：`set_seed(42)` 两次采样结果一致（0.37454）。

## 遇到的问题与决策记录

| 问题 | 决策 |
|------|------|
| pyarrow/pyyaml 在 Python 3.14 无发行版 | 改用 CSV + JSON，不降级 Python（torch 已装好，重装成本高） |
| 代码仓与原始数据分离（英文 vs 中文路径） | 路径集中在 configs/paths.json，代码零硬编码 |

## 检查结论（2026-09-03）

- 通过。追加决策：**现阶段用 CPU，GPU 设为阶段 3 的可选加速项**——触发条件为"网格搜索在 CPU 上预计超过 4 小时"；切换命令与零代码改动说明见讨论记录。理由：数据/模型规模迷你（约 5 万参数、单次训练分钟级），RF/LOF/数据处理本身全在 CPU，GPU 还引入非确定性与 2–3GB 磁盘开销。

## 下一步（阶段 1 预告，待检查通过后执行）

`src/data/` 四个脚本：parse（GBK TXT + 伪 xlsx 元数据 + OriData2 混杂格式）→ clean（999999→NaN、插值规则）→ merge（join 台站元数据）→ split（按时间 8:2），产出 `data/clean/long.csv` 与数据质量一览表。
