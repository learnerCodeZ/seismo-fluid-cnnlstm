# AGENTS.md — 工作指引

本文件供 AI agent / 新协作者快速进入项目工作状态。修改本版本内容前先读完。

## 1. 本项目是什么

大创项目（负责人舒焕，2026.5—2027.5）：**针对双流体（水氡 + 水位）的 CNN-LSTM 联合建模探索**，以 2026-01-19 巧家 MS5.1 地震为参考震例（不限定震例），做震前地下流体异常识别与多参数互证。

方法蓝本：`papers/基于机器学习的水氡异常地震前兆识别_刘慧.pdf`（刘慧等 2026，LSTM-RF 预测 + 残差 LOF 无监督异常检测 + 多参数互证）。精读总结在 `papers/刘慧2026_论文总结.md`。

## 2. 目录约定

```
dachuang_cnn_lstm/       ← 项目根目录（D:\MYCODE\dachuang_cnn_lstm\）
├── src/                 ← Python 源码（data/features/models/anomaly/viz/analysis）
├── configs/             ← 实验配置 JSON（paths.json 集中管理路径）
├── data/clean/          ← 清洗后数据（程序生成，禁止手改）
├── results/             ← 每次实验一个子目录
├── figures/             ← 出版级图件
├── docs/                ← 技术文档（数据方案、技术方案、Web可视化计划）
├── notes/               ← 阶段执行笔记
├── papers/              ← 文献 PDF 及精读笔记
└── proposal/            ← 大创申请书
```

- **原始数据**在 `D:\桌面\地质\大创\dataAnalysis\` 下（OriData/OriData2/OriData3/Papers），**只读不动**，一切清洗产物进 `data/clean/`。
- **解释器**：`D:\桌面\地质\大创\dataAnalysis\.venv\Scripts\python.exe`（Python 3.14.5，torch 2.13.0+cpu 已装）。
- **路径配置**：`configs/paths.json` 分 external（原始数据，相对 data_root）和 internal（产物，相对项目根），代码零硬编码。

## 3. 数据事实与格式陷阱（处理数据前必读）

详细盘点见 `docs/new/数据方案.md` 或 `docs/数据盘点.md`，要点：

- **OriData/**：127 个日值 TXT，GBK 编码，两列 `YYYYMMDD value`；缺测值 `999999`；覆盖 2025-01-01～2026-06-11。
- **OriData/B区域_云南省.xlsx**：台站元数据表（实为单列 CSV 文本，需按逗号拆分并去除值内引号）。
- **测项码**：4111 动水位｜4112 静水位｜4211 溶解气氡｜4212 逸出气氡｜4214 气氡｜4221 溶解汞｜4222 逸出气汞｜4224 气汞｜4232 逸出气氢｜4311/4312/4313 浅/中/深层水温。
- **OriData2/**/*.xlsx**：双列表，**时间列格式混杂**（`20250101` 与 `2026-06-09` 混用），表头名不统一。
- **OriData3/西昌.csv**：西昌站气象三要素日值，GBK 编码，2010-01–2026-08，三要素零缺测。降水 2010–2019 单位为 0.1mm（需 ÷10）；9999/32766 为缺测码。
- **OriData3/earthquake_catalog.csv**：USGS 819 事件，M≥3，2015–2026。
- **重点台站**：西昌32井（SCXC32，水氡 2015–2026 约 11.3 年，方法主战场）；昭通SK3井（53034，四参数 2025–2026，消融主战场）。

## 4. 已定的技术决策（不要回退，除非用户明确要求）

1. **异常检测走无监督路线**：模型预测 → 残差 → LOF。不采用监督分类。
2. **LOF 参照口径**：对残差序列自身打分（自参照 novelty），不用训练段当参照。
3. **NaN 特征行整行丢弃**：禁止插值（会毒化 LOF 密度到 1e9 量级）。
4. **工作阈值由敏感性确定**：不拍脑袋定分位数。
5. **消融实验 A/B/C**：联合建模的实证核心，每组 ≥5 随机种子。
6. 时序实验**按时间顺序划分**，禁止随机打乱。
7. 基线必须含文献同款 RF / LSTM / LSTM-RF。
8. 工具链：Python（PyTorch + scikit-learn + pandas），CSV + JSON（无 pyarrow/pyyaml）。
9. 与 v5.0 衔接：小波系数作为可选特征通道，不做硬耦合。

## 5. 写作与文档惯例

- 文档一律 Markdown、中文；跨文档引用用相对链接。
- 技术文档放 `docs/`，阶段笔记放 `notes/`，文献笔记放 `papers/`。
- 引用文献数据/结论时保留出处，禁止编造数值。
- 项目管理内容（进度/分工/经费）不写入技术文档。

## 6. 已知待办与风险

- 数据缺口：昭通气象、2022–2024 回溯数据、仪器标定记录。
- 申请书待修改（数据来源渠道、依托项目、创新点表述）。
- `proposal/*.doc` 为二进制老格式：需先经 Word COM 转 docx。
